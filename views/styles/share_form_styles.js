import { colors } from '../../constants'

export const shareFormStyles = (theme) => ({
  dialogTitle: {
    backgroundColor: colors.black,
  },
  closeButton: {
    color: colors.neutral_blue,
  },
  submitButton: {
    borderColor: colors.neutral_blue,
    color: colors.white,
    backgroundColor: colors.neutral_blue,
  },
})
